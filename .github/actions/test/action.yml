# action.yml
name: 'Build'
description: 'Build'
inputs:
  license:
    description: 'Unity ulf-file'
    required: true
  mode:
    description: 'Test mode'
    required: true
    default: 'playmode'
  image:
    description: 'Unity image'
    required: true
    default: 'gableroux/unity3d:2019.1.14f1'
runs:
  using: 'docker'
  image: 'Dockerfile'