name: CI

on:
  push:
    branches:    
      - feature/github-ci

jobs:
  build_WebGL:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v1
    - uses: AlexanderDykov/unity-build-action@2019.2.12f1
      with:
        license: ${{ secrets.UNITY_LICENSE_CONTENT }}
        platform: WebGL
        buildName: SpaceWars
    - run: cp -rf ./Builds/WebGL/SpaceWars/ SpaceWars
    - name: Deploy
      uses: JamesIves/github-pages-deploy-action@releases/v2
      env:
        ACCESS_TOKEN: ${{ secrets.GITHUB_PAT }}
        BRANCH: gh-pages
        FOLDER: ${{ secrets.PROJECT_NAME }}
        BASE_BRANCH: master